pipeline {
    agent any

    stages {
        stage('Setup env') {
            steps {
                echo 'Env setup hehe'
            }
        }
        stage('Setup env 2') {
            steps {
                echo 'Env setup hehe'
            }
        }
        stage('Print env') {
            steps {
                sh 'printenv | sort'
            }
        }
    }
}